@page
@model IdeaCollector.Pages.Ideas.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Idea</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Idea.ID" />
            <div class="form-group">
                <label asp-for="Idea.Title" class="control-label"></label>
                <input asp-for="Idea.Title" class="form-control" />
                <span asp-validation-for="Idea.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Idea.ReleaseDate" class="control-label"></label>
                <input asp-for="Idea.ReleaseDate" class="form-control" />
                <span asp-validation-for="Idea.ReleaseDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Idea.Genre" class="control-label"></label>
                <input asp-for="Idea.Genre" class="form-control" />
                <span asp-validation-for="Idea.Genre" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Idea.Price" class="control-label"></label>
                <input asp-for="Idea.Price" class="form-control" />
                <span asp-validation-for="Idea.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
